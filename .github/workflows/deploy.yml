name: Deploy VeriOps to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            set -e

            PROJECT_ROOT="/home/administrator/VeriRevOps"

            cd "$PROJECT_ROOT"
            git fetch origin main
            git reset --hard ${{ github.sha }}

            echo "Rebuilding engine"
            cd "$PROJECT_ROOT/verirevops/engine"
            docker compose up --build -d

            echo "Rebuilding Chatwoot"
            cd "$PROJECT_ROOT/verirevops/chatwoot"
            docker compose up --build -d

            echo "Rebuilding EspoCRM"
            cd "$PROJECT_ROOT/verirevops/espocrm/crm"
            docker compose up --build -d

            docker image prune -f

            echo "Deployment Complete"
